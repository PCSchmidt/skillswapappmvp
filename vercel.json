{
  "version": 2,
  "regions": ["iad1"],
  "buildCommand": "cp vercel-package-minimal.json package.json && npm install && NEXT_TELEMETRY_DISABLED=1 npm run build",
  "env": {
    "NODE_ENV": "production",
    "NEXT_TELEMETRY_DISABLED": "1",
    "SKIP_TYPE_CHECK": "true",
    "NODE_OPTIONS": "--max-old-space-size=4096"
  },
  "github": {
    "enabled": true,
    "silent": false
  }
}
