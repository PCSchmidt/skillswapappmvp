export const formatRelativeTime = (dateString: string): string => {
  // Placeholder implementation for testing purposes
  // In a real application, this would format a date into a relative time string (e.g., "2 hours ago")
  return "2 hours ago";
};
